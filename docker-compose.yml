services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - ./uploads:/app/uploads
      - ./src/database:/app/src/database
    command: >
      sh -c "
        echo '🚀 Starting Vaultify Backend...' &&
        echo '⏳ Waiting for database to be ready...' &&
        sleep 5 &&
        echo '🔄 Running database migrations...' &&
        npm run migration:run || echo '⚠️ Migration failed, continuing anyway...' &&
        echo '🎯 Starting application...' &&
        npm run start:prod
      "
